package dao;

/**
 * DAO: ClienteDAO
 * Responsável: Eúde
 * -----------------------------------------------------------------------------
 * Camada SQL/DAO para o Cadastro de Clientes.
 * - O que fazer em SQL: Criar e gerenciar a tabela `clientes` com restrições de CNPJ/CPF único e limite de crédito.
 * - O que fazer em DAO: Implementar operações JDBC de Insert, Update, Select (por ID e CNPJ) e Delete para persistência dos clientes.
 */
public class ClienteDAO {
    // Métodos de persistência para clientes (inserir, buscar, atualizar).
}
